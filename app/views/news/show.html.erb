<div class="row">
  <div class="box">
    <div class="col-lg-12">
      <hr>
      <h2 class="intro-text text-center"><strong><%= t('help.news_group.group_' + params[:group]) %></strong></h2>
      <hr>
      <h1><%= @news.title %></h1>
      <%= raw(@news.content) %>
      <br>
      <div align="center">
        <input type='button' value='<%= t('views.button.back') %>' class='btn btn-default' onclick='javascript:history.back();'>
      </div>
    </div>
  </div>
</div>